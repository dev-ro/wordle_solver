name: Deploy Firebase Hosting and Rules

on:
  push:
    branches: [main]

jobs:
  deploy-rules:
    name: Deploy Firestore/Storage Rules
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_WORDLE_SOLVER_KYLE }}
      - name: Install Firebase Tools
        run: npm install -g firebase-tools
      - name: Firebase Deploy Rules
        env:
          PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID || 'wordle-solver-kyle' }}
        run: |
          firebase deploy --only firestore,storage --non-interactive --project "$PROJECT_ID"

  deploy-hosting:
    name: Deploy Hosting
    runs-on: ubuntu-latest
    if: ${{ github.event_name == 'push' }}
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.32.8'
          channel: 'stable'
          cache: true
      - run: flutter pub get
      - run: flutter build web --release
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_WORDLE_SOLVER_KYLE }}
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - run: npm install -g firebase-tools
      - name: Deploy to Firebase Hosting
        env:
          PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID || 'wordle-solver-kyle' }}
        run: |
          firebase deploy --only hosting --non-interactive --project "$PROJECT_ID"


