name: Deploy Database Rules

on:
  push:
    branches: [ main ]
    paths: [ 'firestore.rules', 'firestore.indexes.json', 'storage.rules' ]
  workflow_dispatch:

jobs:
  deploy-database:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Install Firebase CLI
      run: |
        curl -sL https://firebase.tools | bash
        export PATH="$PATH:$HOME/.local/bin"
      
    - name: Validate Firestore rules
      run: |
        echo "Validating Firestore security rules..."
        firebase firestore:rules:validate --project wordle-solver-kyle
        echo "✅ Firestore rules are valid"
      env:
        FIREBASE_TOKEN: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_WORDLE_SOLVER_KYLE }}
        
    - name: Deploy database rules using bash script
      run: ./scripts/deploy.sh database
      env:
        FIREBASE_TOKEN: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_WORDLE_SOLVER_KYLE }}
        
    - name: Verify deployment
      run: |
        echo "✅ All database rules deployed successfully"
        echo "🔥 Firestore rules: Updated"
        echo "📁 Storage rules: Updated"
        echo "📊 Firestore indexes: Updated"
